export const checkUniqueEmployeeEmail=(email)=>{

    //fetch all avialable emails of employee so we can match  
    
    }
    
    export const checkUniqueVendorEmail=(email)=>{
    
        //fetch all avialable emails of vendors so we can match  
        
        }